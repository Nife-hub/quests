package main

func Capitalise (s string) string{
	result := ""
	for i := 0; i < len(s); i++ { 
		ch := s[i]
		if i != 0 && s[i-1] == ' ' || s[i-1] < 'a' && s[i-1] > 'z' || s[i-1] < 'A' && s[i-1] > 'Z' [

		]

			 
		}
	}
